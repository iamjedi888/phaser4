import { IFrame } from '../../../textures/IFrame';
import { IRenderPass } from '../renderpass/IRenderPass';
import { ITexture } from '../../../textures/ITexture';
export declare function DrawFrame<T extends ITexture>(renderPass: IRenderPass, texture: T, frame: string | number | IFrame, x: number, y: number, alpha?: number, scaleX?: number, scaleY?: number): void;
//# sourceMappingURL=DrawFrame.d.ts.map