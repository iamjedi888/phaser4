import { IRenderPass } from '../renderpass/IRenderPass';
import { ITexture } from '../../../textures/ITexture';
export declare function DrawTiles<T extends ITexture>(renderPass: IRenderPass, texture: T, tileWidth: number, tileHeight: number, mapData: number[], mapWidth: number, x?: number, y?: number, alpha?: number): void;
//# sourceMappingURL=DrawTiles.d.ts.map